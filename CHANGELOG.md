## 0.0.2

* `FlutterDove.initialize` accepts an optional `deviceId` to override the
  package's default persisted UUID; omit it to keep the existing
  generate-and-persist behavior.

## 0.0.1

* Initial release: `FlutterDove` for device/FCM token registration, user
  identification, topic subscriptions, and incoming-message streams against
  an `ost-push` backend.
