name: flutter_dove_example
description: "Demonstrates how to use the flutter_dove package."
publish_to: "none"
version: 0.0.1

environment:
  sdk: ^3.13.1

dependencies:
  flutter:
    sdk: flutter
  flutter_dove:
    path: ../

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
